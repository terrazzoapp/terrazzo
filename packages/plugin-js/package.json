{
  "name": "@cobalt-ui/plugin-js",
  "description": "Generate JS, TS, and JSON from your design tokens schema (requires @cobalt-ui/cli)",
  "version": "1.4.3",
  "author": {
    "name": "Drew Powers",
    "email": "drew@pow.rs"
  },
  "keywords": [
    "cobalt",
    "design tokens",
    "design tokens community group",
    "design tokens format module",
    "design system",
    "dtcg",
    "w3c design tokens"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drwpow/cobalt-ui.git",
    "directory": "packages/plugin-js"
  },
  "homepage": "https://cobalt-ui.pages.dev",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "pnpm run build:clean && pnpm run build:ts && pnpm run build:license",
    "build:clean": "del dist",
    "build:ts": "tsc",
    "build:license": "node ../../scripts/inject-license.js @cobalt-ui/plugin-js dist/index.js",
    "dev": "tsc -w",
    "test": "co build -c ./test/types.tokens.mjs && pnpm run --parallel \"/^test:.*/\"",
    "test:js": "vitest run",
    "test:ts": "tsc --noEmit"
  },
  "peerDependencies": {
    "@cobalt-ui/cli": "1.x"
  },
  "dependencies": {
    "@cobalt-ui/utils": "^1.2.2"
  },
  "devDependencies": {
    "@cobalt-ui/cli": "^1.6.2",
    "@cobalt-ui/core": "^1.6.1",
    "fast-glob": "^3.3.2",
    "vitest": "^1.2.0"
  }
}
