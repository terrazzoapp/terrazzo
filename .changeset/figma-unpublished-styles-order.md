---
"@terrazzo/cli": minor
---

Apply `--unpublished` to Figma Style import, so unpublished Styles are imported alongside published ones. `importFromFigma` accepts a `resolutionOrder` option to preserve an existing order instead of the discovery order.
