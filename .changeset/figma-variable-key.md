---
"@terrazzo/cli": patch
---

Preserve each Figma variable’s `key` beside its `id` in `$extensions["figma.com"]` on import, so exported tokens can be joined back to the variable after a republish or from another file
