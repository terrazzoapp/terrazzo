---
"@terrazzo/plugin-swift": patch
---

Emit exact zero and one Swift asset-catalog color components as decimal strings (`"0.0"` and `"1.0"`) so Xcode treats them as normalized values instead of 8-bit integers.
