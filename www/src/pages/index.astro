---
import Cobalt from '../components/Cobalt.astro';
import Footer from '../components/Footer.astro';
import Icons from '../components/Icons.astro';
import Head from '../components/Head.astro';
import Nav from '../components/Nav.astro';
import SkipToMain from '../components/SkipToMain.astro'
---

<!DOCTYPE html>
<html>
  <head>
    <Head title="Cobalt" />
    <style lang="scss">
      @use "../../tokens" as *;

      .header {
        align-items: center;
        display: flex;
        flex-direction: column;
        margin: 8em 0;
      }

      .title {
        font-weight: 500;
        margin: 4rem 0;
      }

      h2 {
        font-size: 32pt;
      }

      section + section {
        margin-top: 4rem;
      }

      p {
        font-size: 16pt;
        line-height: 1.7;
      }

      figure {
        margin: 0;
      }

      .split {
        display: grid;
        grid-template-columns: 3fr 4fr;
        grid-gap: 2.5rem;

        p:first-of-type {
          margin-top: 0;
        }
      }

      .visual {
        align-self: start;
        font-size: 18pt;
        font-weight: 500;
        position: relative;
      }

      .visual-layer {
        border-bottom: 1px dashed $color__white;
        margin-bottom: 2rem;
        padding-bottom: 2rem;
      }

      .visual-base {
        height: 0;
        padding-top: 36%;
        position: relative;
      }

      .visual-cobalt,
      .visual-tokens {
        transform: translateY(-50%);
        position: absolute;
      }

      .visual-line-c,
      .visual-line-t {
        border-top: 1px dashed $color__white;
        display: block;
        left: 6.5rem;
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        width: calc(41% - 6.5rem);
      }

      .visual-line-t {
        border-top: 3px solid $color__white;
        top: 78%;
        width: calc(49% - 6.5rem);
      }

      .visual-cobalt {
        top: 50%;
      }

      .visual-tokens {
        top: 78%;
      }

      .visual-img {
        bottom: 0;
        height: auto;
        position: absolute;
        right: 0;
        width: 60%;
      }

      .actions-title {
        margin-bottom: 0;
      }

      .actions {
        align-items: flex-start;
        display: flex;
        flex-direction: column;
        padding-left: 2.5rem;
        position: relative;

        &::after {
          border-right: 1px dashed $color__white;
          content: "";
          display: block;
          height: calc(100% - 2.5rem);
          left: 0.5rem;
          position: absolute;
          top: 1rem;
        }

        a {
          margin-top: 1em;
          position: relative;

          &::before {
            border-top: 1px dashed $color__white;
            content: "";
            display: block;
            position: absolute;
            right: calc(100% + 4px);
            top: 50%;
            transform: translateY(-50%);
            width: 1.75rem;
          }
        }
      }
    </style>
  </head>
  <body>
    <SkipToMain />
    <Icons />
    <Nav />
    <div class="wrapper">
      <header class="header tac">
        <Cobalt />
        <h1 class="title">Automated design tokens</h1>
      </header>
      <main id="#main">
        <section>
          <h2>What is Cobalt?</h2>
          <p>
            <strong>Cobalt is not a design system.</strong> Cobalt has no
            out-of-the-box colors, typography, or symbology. All that is for you
            to define.
          </p>
          <p>
            <strong>Cobalt is token management.</strong> It lets you organize
            and categorize your tokens in a way that makes sense to you. But it
            revolves around Cobalt Schema v1, a standard format for storage.
            From your custom design tokens schema you can use plugins to
            generate code from your design tokens, from CSS/Sass to
            TypeScript/JavaScript to univeral JSON for backend code and APIs.
          </p>
          <div class="split">
            <div>
              <p>
                Thinking about your product design in “layers,” Cobalt sits
                below your design system, and beneath your UI. You still control
                your tokens; Cobalt only provides “buckets“ to put them in.
              </p>
              <p>
                In return you don’t have to build or manage custom software for
                your design tokens like so many teams often do. And you still have
                the flexibility to craft your design system in any way you’d like.
                Cobalt just bridges the gap between your tokens, and your design
                system/UI.
              </p>
            </div>
            <figure class="visual">
              <div class="visual-layers">
                <div class="visual-layer">UI</div>
                <div class="visual-layer">Platform</div>
                <div class="visual-layer">Design System</div>
                <div class="visual-base">
                  <span class="visual-cobalt">Cobalt</span>
                  <span class="visual-tokens">Tokens</span>
                  <div class="visual-line-c"></div>
                  <div class="visual-line-t"></div>
                  <img class="visual-img" src="/token-hex.svg">
                </div>
              </div>
            </figure>
          </div>
        </section>
        <section>
          <h2>Try it out</h2>
          <p>Cobalt is open source and free to use today!</p>
          <h3 class="actions-title">Actions</h3>
          <menu class="actions">
            <a href="/editor" class="btn btn--icon">Try the online editor <svg><use xlink:href="#arrow-r"></use></svg></a>
            <a href="/docs" class="btn btn--icon">Read documentation <svg><use xlink:href="#arrow-r"></use></svg></a>
            <a href="https://github.com/drwpow/cobalt-ui/blob/main/examples/" class="btn btn--icon">See examples <svg><use xlink:href="#arrow-r"></use></svg></a>
          </menu>
        </section>
      </main>
    </div>
    <Footer />
  </body>
</html>
